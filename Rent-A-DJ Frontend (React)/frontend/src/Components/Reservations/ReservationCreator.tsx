import { useState } from "react";
import { useUserContext } from "../../context";

export const ReservationCreator:React.FC = () => {

    const user = useUserContext();

    const[newRes, setNewRes] = useState({
        
    })

    return(
        <>
        </>
    )
}